name: Hello Pipeline

on:
  workflow_dispatch:   # allows manual trigger

jobs:
  say-hello:
    runs-on: [self-hosted, ip-10-0-6-171]   # use your self-hosted runner
    steps:
      - name: Print Hello
        run: echo "Hello from my self-hosted runner (ip-10-0-6-171)!"
