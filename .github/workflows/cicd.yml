name: Directory and File Creation

on:
  workflow_dispatch:

jobs:
  create-dir-file:
    runs-on: self-hosted
    steps:
      - name: Create directory
        run: mkdir -p /home/runner-test/demo-dir

      - name: Create file with content
        run: echo "This file was created by GitHub Actions on self-hosted runner" > /home/runner-test/demo-dir/hello.txt

      - name: Display file content
        run: cat /home/runner-test/demo-dir/hello.txt
