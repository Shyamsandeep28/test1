name: Hello Pipeline

on:
  workflow_dispatch:   # allows manual trigger

jobs:
  say-hello:
    runs-on: self-hosted
    steps:
      - name: Print Hello
        run: echo "Hello from my self-hosted runner (ip-10-0-6-171)!"
