name: Run on Tag

on:
  push:
    tags:
      - "v*"   # Triggers only when a tag like v1.0, v2.0 is pushed

jobs:
  run-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Hello Script
        run: python hello.py
