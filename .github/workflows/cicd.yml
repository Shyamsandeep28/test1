name: Expression Demo

on: [push]

jobs:
  conditional-job:
    runs-on: ubuntu-latest
    steps:
      - name: Run always
        run: echo "This runs always!"

      - name: Run only on dev1 branch
        if: github.ref == 'refs/heads/dev1'
        run: echo "This runs only on main branch 1"
        
        
